---
status: draft
---

# Keyboard Shortcuts

<A few short paragraphs: the problem, the goals / non-goals, and a rough approach. Concrete but not exhaustive — the detailed design is generated later by /mp-spec-expander. Reference existing files and conventions rather than inventing new ones.>

> [!NOTE]
> `Ctrl-S`/`Cmd-S` is already claimed by the `autosave-with-revisions` feature: inside an
> autosaving field it flushes the pending save and closes the current coalescing window
> (see `documentation/architecture.md` → "Revisions"). Whichever shortcut scheme this spec
> lands on should treat that binding as spoken for rather than reassigning it.
