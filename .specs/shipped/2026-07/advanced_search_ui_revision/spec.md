---
status: shipped
shipped: 2026-07-16
---

The search page must no longer use columns, which ended up too narrow for optimal reading.

Instead, change each column to a table list (much like entity list pages) with a view button.

Columns are:

* Name (with a link to the entity)
* fields matched, concatenated and separated by ", " (ie "boar" would match scene "the boar hunt" twice)
* the text preview 
* the actions
