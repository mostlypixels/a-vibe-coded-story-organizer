---
status: planned
---

# Empty states

Index pages render a bare "no results" row when a collection is empty.

* Friendly empty-state copy on the Codex index (characters/locations/organizations)
  when a type has no entries, pointing at the create action.
* Same treatment for the events/acts/chapters/scenes indexes via a shared
  `x-table-empty` message.
